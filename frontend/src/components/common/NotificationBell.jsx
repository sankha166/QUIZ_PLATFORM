import { useState, useRef, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { useNotifications } from '../../hooks/useNotifications';
function timeAgo(dateStr){const diff=Date.now()-new Date(dateStr).getTime();const m=Math.floor(diff/60000);if(m<1)return'just now';if(m<60)return`${m}m ago`;const h=Math.floor(m/60);if(h<24)return`${h}h ago`;return`${Math.floor(h/24)}d ago`}
export default function NotificationBell(){const{notifications,unreadCount,markRead,markAllRead,deleteNotification}=useNotifications();const[open,setOpen]=useState(false);const ref=useRef(null);const navigate=useNavigate();useEffect(()=>{const handler=e=>{if(ref.current&&!ref.current.contains(e.target))setOpen(false)};document.addEventListener('mousedown',handler);return()=>document.removeEventListener('mousedown',handler)},[]);const handleNotifClick=n=>{if(!n.is_read)markRead(n.id);if(n.data?.attemptId){navigate(`/student/results/${n.data.attemptId}`);setOpen(false)}else if(n.data?.quizId){navigate(`/student/quizzes/${n.data.quizId}`);setOpen(false)}};return <div className="relative" ref={ref}><button onClick={()=>setOpen(!open)} className="relative inline-flex h-10 w-10 items-center justify-center rounded-xl text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-300 dark:hover:bg-slate-800" aria-label="Notifications"><svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6 6 0 00-5-5.917V4a1 1 0 10-2 0v1.083A6 6 0 006 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/></svg>{unreadCount>0&&<span className="absolute right-0 top-0 flex h-4 min-w-4 items-center justify-center rounded-full bg-red-500 px-1 text-[10px] font-bold text-white">{unreadCount>9?'9+':unreadCount}</span>}</button>{open&&<div className="fixed inset-x-3 top-[76px] z-[70] mx-auto max-w-md overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl dark:border-slate-700 dark:bg-slate-900 sm:absolute sm:inset-x-auto sm:right-0 sm:top-full sm:mt-2 sm:w-[min(24rem,calc(100vw-2rem))] sm:mx-0"><div className="flex items-center justify-between gap-3 border-b border-slate-100 bg-slate-50 px-4 py-3 dark:border-slate-800 dark:bg-slate-800"><span className="text-sm font-semibold text-slate-800 dark:text-slate-100">Notifications</span>{unreadCount>0&&<button onClick={markAllRead} className="text-xs font-medium text-indigo-600 hover:text-indigo-800 dark:text-indigo-300">Mark all read</button>}</div><div className="max-h-[min(70vh,28rem)] overflow-y-auto">{notifications.length===0?<div className="py-10 text-center text-sm text-slate-400">No notifications yet</div>:notifications.map(n=><div key={n.id} className={`flex items-start gap-3 border-b border-slate-100 px-4 py-3 dark:border-slate-800 ${!n.is_read?'bg-indigo-50/60 dark:bg-indigo-950/30':''}`}><button className="min-w-0 flex-1 text-left" onClick={()=>handleNotifClick(n)}><div className="flex items-start gap-2"><span className={`mt-1.5 h-2 w-2 shrink-0 rounded-full ${!n.is_read?'bg-indigo-500':'bg-transparent'}`}/><div className="min-w-0"><p className={`text-sm font-medium ${!n.is_read?'text-slate-900 dark:text-white':'text-slate-600 dark:text-slate-300'}`}>{n.title}</p><p className="mt-0.5 break-words text-xs leading-5 text-slate-500 dark:text-slate-400">{n.message}</p><p className="mt-1 text-[10px] text-slate-400">{timeAgo(n.created_at)}</p></div></div></button><button onClick={e=>{e.stopPropagation();deleteNotification(n.id)}} className="shrink-0 rounded-lg px-2 py-1 text-lg leading-none text-slate-300 hover:bg-slate-100 hover:text-red-500 dark:hover:bg-slate-800" aria-label="Delete notification">×</button></div>)}</div></div>}</div>}
