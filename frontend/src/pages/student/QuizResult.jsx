import {useEffect,useMemo,useState} from 'react';import {useParams,Link} from 'react-router-dom';import {getAttemptById} from '../../api/attempt.api';import {submitQuizReview} from '../../api/reviews.api';import StudentLayout from '../../components/student/StudentLayout';import LoadingSpinner from '../../components/common/LoadingSpinner';import Badge from '../../components/common/Badge';import {formatTime,formatDate,statusColor} from '../../utils/helpers';
function qt(s){s=Number(s)||0;return s<60?`${s}s`:`${Math.floor(s/60)}m ${s%60}s`}
const moods=[['love','😍','Amazing!'],['happy','😊','Great!'],['okay','🙂','Good'],['sad','😐','Could be better'],['angry','😞','Not satisfied']];
export default function QuizResult(){const{attemptId}=useParams();const[attempt,setAttempt]=useState(null),[loading,setLoading]=useState(true),[reviewOpen,setReviewOpen]=useState(true),[mood,setMood]=useState(''),[review,setReview]=useState(''),[sending,setSending]=useState(false),[reviewSent,setReviewSent]=useState(false);useEffect(()=>{getAttemptById(attemptId).then(r=>setAttempt(r.data.attempt)).catch(console.error).finally(()=>setLoading(false))},[attemptId]);const stats=useMemo(()=>{const t=attempt?.answers?.map(a=>Number(a.time_taken)||0)||[];return{total:t.length,avg:t.length?Math.round(t.reduce((a,b)=>a+b,0)/t.length):0,fastest:t.length?Math.min(...t):0}},[attempt]);const sendReview=async()=>{if(!mood)return;setSending(true);try{await submitQuizReview({attemptId:Number(attemptId),mood,review});setReviewSent(true);setReviewOpen(false)}catch(e){console.error(e)}finally{setSending(false)}};if(loading)return <StudentLayout><LoadingSpinner size="lg" className="py-20"/></StudentLayout>;if(!attempt)return <StudentLayout><p className="py-20 text-center text-red-600">Result not found.</p></StudentLayout>;const passed=attempt.status==='passed';return <StudentLayout><div className="relative max-w-5xl mx-auto space-y-6 min-w-0"><div className={`rounded-3xl border p-5 sm:p-8 ${passed?'border-emerald-200 bg-gradient-to-br from-emerald-50 via-white to-cyan-50':'border-rose-200 bg-gradient-to-br from-rose-50 via-white to-orange-50'}`}><div className="text-center"><div className="text-5xl">{passed?'🏆':'📚'}</div><p className="mt-3 text-xs uppercase tracking-[.25em] font-semibold text-indigo-600">Assessment complete</p><h1 className="mt-2 text-2xl sm:text-3xl font-extrabold break-words">{attempt.quiz_title}</h1><div className="mt-4 flex flex-wrap justify-center gap-2"><Badge className={`px-4 py-2 ${statusColor(attempt.status)}`}>{passed?'✓ PASSED':'✕ FAILED'}</Badge><span className="rounded-full bg-white/80 border px-4 py-2 text-sm text-slate-600">{formatDate(attempt.completed_at)}</span></div><p className={`mt-6 text-5xl sm:text-7xl font-black ${passed?'text-emerald-600':'text-rose-600'}`}>{parseFloat(attempt.percentage).toFixed(1)}<span className="text-2xl">%</span></p></div><div className="mt-7 grid grid-cols-2 sm:grid-cols-4 gap-3">{[['Correct',attempt.correct_answers,'text-emerald-600'],['Incorrect',attempt.incorrect_answers,'text-rose-600'],['Unanswered',attempt.unanswered,'text-slate-700'],['Total time',formatTime(attempt.time_taken),'text-indigo-600']].map(([l,v,c])=><div key={l} className="rounded-2xl bg-white/90 border p-4"><p className="text-xs text-slate-500">{l}</p><p className={`mt-1 text-2xl font-bold ${c}`}>{v}</p></div>)}</div><div className="mt-4 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-indigo-50 border p-4"><p className="text-xs text-indigo-600">Avg. per question</p><p className="font-bold text-indigo-800">{qt(stats.avg)}</p></div><div className="rounded-2xl bg-cyan-50 border p-4"><p className="text-xs text-cyan-700">Fastest question</p><p className="font-bold text-cyan-800">{qt(stats.fastest)}</p></div></div><div className="mt-6 flex flex-col sm:flex-row gap-3"><Link to="/student/quizzes" className="btn-secondary flex-1 text-center">Explore more quizzes</Link><Link to="/student/attempts" className="btn-primary flex-1 text-center">View my attempts</Link></div></div><div><h2 className="text-xl sm:text-2xl font-bold">Question Review</h2><p className="text-sm text-slate-500 mt-1">See your answer, correctness and time spent on each question.</p><div className="mt-4 space-y-3">{attempt.answers?.map((a,i)=><div key={a.id} className={`rounded-2xl border bg-white p-4 sm:p-6 shadow-sm border-l-4 ${a.is_correct?'border-l-emerald-500':a.selected_option_id?'border-l-rose-500':'border-l-slate-300'}`}><div className="flex flex-col sm:flex-row sm:justify-between gap-3"><p className="font-semibold leading-6"><span className="text-slate-400 mr-2">Q{i+1}.</span>{a.question_text}</p><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold self-start">⏱ {qt(a.time_taken)}</span></div><div className="mt-4 space-y-3 text-sm"><div className="rounded-xl bg-slate-50 p-3"><span className="text-slate-500">Your answer: </span><span className={`font-semibold ${a.is_correct?'text-emerald-700':'text-rose-600'}`}>{a.selected_option_text||'Not answered'} {a.is_correct?'✓':''}</span></div>{!a.is_correct&&<div className="rounded-xl bg-emerald-50 border p-3"><span className="text-emerald-700">Correct answer: </span><b>{a.correct_option_text}</b></div>}{a.explanation&&<div className="rounded-xl bg-indigo-50 border p-4"><b className="text-indigo-700">💡 Explanation</b><p className="mt-1 text-slate-600">{a.explanation}</p></div>}</div></div>)}</div></div>{reviewOpen&&!reviewSent&&<div className="fixed inset-0 z-[80] grid place-items-center bg-slate-950/50 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-3xl bg-white p-6 sm:p-8 shadow-2xl"><div className="text-center"><div className="text-4xl">💬</div><h2 className="mt-2 text-2xl font-bold">Quick review</h2><p className="mt-1 text-sm text-slate-500">How did this quiz feel for you?</p></div><div className="mt-6 grid grid-cols-5 gap-1">{moods.map(([id,emoji,label])=><button key={id} type="button" onClick={()=>setMood(id)} className={`rounded-2xl p-2 transition ${mood===id?'bg-indigo-100 ring-2 ring-indigo-500':'hover:bg-slate-50'}`}><span className="block text-3xl">{emoji}</span><span className="mt-1 block text-[10px] font-semibold text-slate-500">{label}</span></button>)}</div><textarea maxLength={1000} value={review} onChange={e=>setReview(e.target.value)} placeholder="Tell us what you liked or what we can improve…" className="input mt-5 h-28 resize-none"/><p className="mt-1 text-right text-xs text-slate-400">{review.length}/1000</p><div className="mt-5 flex gap-3"><button type="button" onClick={()=>setReviewOpen(false)} className="btn-secondary flex-1">Skip</button><button type="button" disabled={!mood||sending} onClick={sendReview} className="btn-primary flex-1">{sending?'Sending…':'Submit review'}</button></div></div></div>}</div></StudentLayout>}
