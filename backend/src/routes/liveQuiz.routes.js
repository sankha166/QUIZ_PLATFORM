const router=require('express').Router();const {query}=require('../config/db');const authenticate=require('../middleware/auth');const studentOnly=require('../middleware/studentOnly');const adminOnly=require('../middleware/adminOnly');
const liveEndSql=`COALESCE(q.live_end_at,q.live_start_at + (SELECT COALESCE(SUM(GREATEST(COALESCE(qu.time_limit_seconds,30),5)),0)*INTERVAL '1 second' FROM questions qu WHERE qu.quiz_id=q.id))`;
const liveEndSqlZ=`COALESCE(z.live_end_at,z.live_start_at + (SELECT COALESCE(SUM(GREATEST(COALESCE(qu.time_limit_seconds,30),5)),0)*INTERVAL '1 second' FROM questions qu WHERE qu.quiz_id=z.id))`;
router.get('/',authenticate,async(req,res,next)=>{try{const admin=req.user.role==='ADMIN';const params=[];let domainFilter='';if(!admin){params.push(req.user.id);domainFilter=`AND (q.live_all_domains=true OR c.domain_id=(SELECT preferred_domain_id FROM users WHERE id=$1) OR (SELECT preferred_domain_id FROM users WHERE id=$1) IS NULL)`}const r=await query(`SELECT q.*,c.name category_name,d.name domain_name,COUNT(DISTINCT a.id)::int attempt_count,COUNT(DISTINCT lqr.id)::int registration_count,COUNT(DISTINCT qs.id)::int question_count,${liveEndSql} AS computed_end_at FROM quizzes q LEFT JOIN categories c ON c.id=q.category_id LEFT JOIN domains d ON d.id=c.domain_id LEFT JOIN questions qs ON qs.quiz_id=q.id LEFT JOIN attempts a ON a.quiz_id=q.id AND a.status!='in_progress' LEFT JOIN live_quiz_registrations lqr ON lqr.quiz_id=q.id WHERE q.is_live_quiz=true ${admin?'':`AND q.status='published'`} ${domainFilter} GROUP BY q.id,c.name,d.name ORDER BY q.live_start_at ASC`,params);res.json({success:true,quizzes:r.rows})}catch(e){next(e)}});
router.get('/stats',authenticate,async(req,res,next)=>{try{const r=await query(`SELECT COUNT(*)::int total,COUNT(*) FILTER(WHERE status='draft')::int drafts,COUNT(*) FILTER(WHERE status='published' AND live_start_at <= (NOW() AT TIME ZONE 'Asia/Kolkata') AND ${liveEndSql}> (NOW() AT TIME ZONE 'Asia/Kolkata'))::int live_now,COUNT(*) FILTER(WHERE status='published' AND live_start_at > (NOW() AT TIME ZONE 'Asia/Kolkata'))::int upcoming,COUNT(*) FILTER(WHERE status='published' AND ${liveEndSql} <= (NOW() AT TIME ZONE 'Asia/Kolkata'))::int completed FROM quizzes q WHERE is_live_quiz=true`);if(req.user.role==='ADMIN'){const a=await query(`SELECT COUNT(*)::int attempts,COALESCE(AVG(live_rating),0)::numeric(10,2) avg_rating FROM attempts a JOIN quizzes q ON q.id=a.quiz_id WHERE q.is_live_quiz=true AND a.status!='in_progress'`);return res.json({success:true,stats:{...r.rows[0],...a.rows[0]}})}const a=await query(`SELECT COUNT(*)::int attempted,COALESCE(SUM(live_rating),0)::numeric(10,2) rating FROM attempts a JOIN quizzes q ON q.id=a.quiz_id WHERE q.is_live_quiz=true AND a.user_id=$1 AND a.status!='in_progress'`,[req.user.id]);const rank=await query(`SELECT COUNT(*)::int+1 rank FROM (SELECT user_id,SUM(live_rating) total_rating FROM attempts a JOIN quizzes q ON q.id=a.quiz_id WHERE q.is_live_quiz=true AND a.status!='in_progress' GROUP BY user_id) s WHERE s.total_rating>(SELECT COALESCE(SUM(live_rating),0) FROM attempts WHERE user_id=$1 AND status!='in_progress' AND quiz_id IN (SELECT id FROM quizzes WHERE is_live_quiz=true))`,[req.user.id]);res.json({success:true,stats:{attempted:a.rows[0].attempted,rating:a.rows[0].rating,overall_rank:rank.rows[0].rank,upcoming:r.rows[0].upcoming}})}catch(e){next(e)}});
router.get('/:quizId/stats',authenticate,async(req,res,next)=>{try{const q=await query(`SELECT q.*,${liveEndSql} AS computed_end_at FROM quizzes q WHERE q.id=$1 AND q.is_live_quiz=true`,[req.params.quizId]);if(!q.rows.length)return res.status(404).json({success:false,message:'Live quiz not found'});const quiz=q.rows[0];const a=await query(`SELECT COUNT(*)::int attempts,COUNT(DISTINCT user_id)::int students,COALESCE(SUM(live_rating),0)::numeric(10,2) total_rating,COALESCE(AVG(live_rating),0)::numeric(10,2) avg_rating,COALESCE(MAX(live_rating),0)::numeric(10,2) top_rating FROM attempts WHERE quiz_id=$1 AND status!='in_progress'`,[quiz.id]);const r=await query('SELECT COUNT(*)::int registrations FROM live_quiz_registrations WHERE quiz_id=$1',[quiz.id]);let student={rating:0,rank:null};if(req.user.role==='STUDENT'){const mine=await query('SELECT COALESCE(live_rating,0) rating FROM attempts WHERE quiz_id=$1 AND user_id=$2 AND status!=\'in_progress\'',[quiz.id,req.user.id]);student.rating=mine.rows[0]?.rating||0;const rank=await query(`SELECT COUNT(*)::int+1 rank FROM attempts WHERE quiz_id=$1 AND status!='in_progress' AND live_rating>COALESCE($2,0)`,[quiz.id,student.rating]);student.rank=rank.rows[0].rank}res.json({success:true,stats:{id:quiz.id,title:quiz.title,status:quiz.status,live_start_at:quiz.live_start_at,computed_end_at:quiz.computed_end_at,...a.rows[0],...r.rows[0],...student}})}catch(e){next(e)}});
router.post('/:quizId/register',authenticate,studentOnly,async(req,res,next)=>{try{const r=await query(`SELECT q.id FROM quizzes q LEFT JOIN categories c ON c.id=q.category_id WHERE q.id=$1 AND q.is_live_quiz=true AND q.status='published' AND q.live_start_at>(NOW() AT TIME ZONE 'Asia/Kolkata') AND (q.live_all_domains=true OR (SELECT preferred_domain_id FROM users WHERE id=$2) IS NULL OR c.domain_id=(SELECT preferred_domain_id FROM users WHERE id=$2))`,[req.params.quizId,req.user.id]);if(!r.rows.length)return res.status(400).json({success:false,message:'Registration is not available for this live quiz'});await query(`INSERT INTO live_quiz_registrations(quiz_id,user_id) VALUES($1,$2) ON CONFLICT(quiz_id,user_id) DO NOTHING`,[req.params.quizId,req.user.id]);res.json({success:true,registered:true})}catch(e){next(e)}});
router.get('/:quizId/registration',authenticate,studentOnly,async(req,res,next)=>{try{const r=await query('SELECT 1 FROM live_quiz_registrations WHERE quiz_id=$1 AND user_id=$2',[req.params.quizId,req.user.id]);res.json({success:true,registered:r.rows.length>0})}catch(e){next(e)}});
router.post('/:quizId/start',authenticate,studentOnly,async(req,res,next)=>{try{const r=await query(`SELECT q.*,c.domain_id,${liveEndSql} AS computed_end_at FROM quizzes q LEFT JOIN categories c ON c.id=q.category_id WHERE q.id=$1 AND q.is_live_quiz=true AND q.status='published' AND q.live_start_at <= (NOW() AT TIME ZONE 'Asia/Kolkata') AND ${liveEndSql} > (NOW() AT TIME ZONE 'Asia/Kolkata')`,[req.params.quizId]);if(!r.rows.length)return res.status(400).json({success:false,message:'This live quiz is not live right now'});const domain=await query('SELECT preferred_domain_id FROM users WHERE id=$1',[req.user.id]);if(!r.rows[0].live_all_domains&&domain.rows[0]?.preferred_domain_id&&String(domain.rows[0].preferred_domain_id)!==String(r.rows[0].domain_id))return res.status(403).json({success:false,message:'This live quiz is outside your selected domain'});let attempt=await query(`SELECT id FROM attempts WHERE quiz_id=$1 AND user_id=$2 AND status='in_progress'`,[req.params.quizId,req.user.id]);let attemptId;if(attempt.rows.length)attemptId=attempt.rows[0].id;else{const ins=await query(`INSERT INTO attempts(quiz_id,user_id,status,expiry_time) VALUES($1,$2,'in_progress',$3) RETURNING id`,[req.params.quizId,req.user.id,new Date(Date.now()+86400000)]);attemptId=ins.rows[0].id}const questions=await query(`SELECT q.*,json_agg(json_build_object('id',o.id,'option_text',o.option_text) ORDER BY o.id) AS options FROM questions q LEFT JOIN options o ON o.question_id=q.id WHERE q.quiz_id=$1 GROUP BY q.id ORDER BY q.id`,[req.params.quizId]);res.json({success:true,attemptId,quiz:r.rows[0],questions:questions.rows})}catch(e){next(e)}});
router.post('/:quizId/answer',authenticate,studentOnly,async(req,res,next)=>{try{const{attemptId,questionId,optionId,timeTaken}=req.body;const q=await query(`SELECT qu.id,qu.marks,qu.time_limit_seconds,(SELECT id FROM options WHERE question_id=qu.id AND is_correct=true LIMIT 1) correct_option_id FROM questions qu JOIN quizzes z ON z.id=qu.quiz_id WHERE qu.id=$1 AND qu.quiz_id=$2 AND z.is_live_quiz=true AND z.status='published' AND z.live_start_at <= (NOW() AT TIME ZONE 'Asia/Kolkata') AND ${liveEndSqlZ} > (NOW() AT TIME ZONE 'Asia/Kolkata')`,[questionId,req.params.quizId]);if(!q.rows.length)return res.status(404).json({success:false,message:'Live quiz is not active'});const attempt=await query(`SELECT id FROM attempts WHERE id=$1 AND quiz_id=$2 AND user_id=$3 AND status='in_progress'`,[attemptId,req.params.quizId,req.user.id]);if(!attempt.rows.length)return res.status(404).json({success:false,message:'Live attempt not found'});const limit=Math.max(5,Number(q.rows[0].time_limit_seconds)||30),t=Math.max(0,Number(timeTaken)||0);if(t>limit+2)return res.status(400).json({success:false,message:'Question time expired'});const correct=String(optionId)===String(q.rows[0].correct_option_id);let multiplier=0;if(correct){const ratio=t/limit;multiplier=ratio<=.25?2:ratio<=.5?1.5:ratio<=.75?1.25:1}else if(optionId)multiplier=-.5;const rating=Number((Number(q.rows[0].marks||1)*multiplier).toFixed(2));await query(`INSERT INTO answers(attempt_id,question_id,selected_option_id,is_correct,time_taken,response_at) VALUES($1,$2,$3,$4,$5,NOW()) ON CONFLICT(attempt_id,question_id) DO UPDATE SET selected_option_id=EXCLUDED.selected_option_id,is_correct=EXCLUDED.is_correct,time_taken=EXCLUDED.time_taken,response_at=NOW()`,[attemptId,questionId,optionId||null,correct,t]);await query(`UPDATE attempts SET live_rating=COALESCE((SELECT SUM(CASE WHEN a.is_correct=true THEN q.marks*(CASE WHEN a.time_taken<=q.time_limit_seconds*.25 THEN 2 WHEN a.time_taken<=q.time_limit_seconds*.5 THEN 1.5 WHEN a.time_taken<=q.time_limit_seconds*.75 THEN 1.25 ELSE 1 END) WHEN a.selected_option_id IS NULL THEN 0 ELSE -q.marks*.5 END) FROM answers a JOIN questions q ON q.id=a.question_id WHERE a.attempt_id=$1),0) WHERE id=$2`,[attemptId,attemptId]);const dist=await query(`SELECT o.id,COUNT(a.id)::int chosen FROM options o LEFT JOIN answers a ON a.question_id=o.question_id AND a.selected_option_id=o.id AND a.attempt_id IN (SELECT id FROM attempts WHERE quiz_id=$1) WHERE o.question_id=$2 GROUP BY o.id ORDER BY o.id`,[req.params.quizId,questionId]);res.json({success:true,correct,correctOptionId:q.rows[0].correct_option_id,rating,multiplier,distribution:dist.rows})}catch(e){next(e)}});
router.post('/:quizId/finish',authenticate,studentOnly,async(req,res,next)=>{try{const a=await query(`SELECT id FROM attempts WHERE id=$1 AND quiz_id=$2 AND user_id=$3 AND status='in_progress'`,[req.body.attemptId,req.params.quizId,req.user.id]);if(!a.rows.length)return res.status(404).json({success:false,message:'Live attempt not found'});const rating=await query('SELECT COALESCE(live_rating,0) rating FROM attempts WHERE id=$1',[req.body.attemptId]);await query(`UPDATE attempts SET status='passed',score=ROUND(COALESCE(live_rating,0)),percentage=NULL,completed_at=NOW() WHERE id=$1`,[req.body.attemptId]);const rank=await query(`SELECT COUNT(*)::int+1 rank FROM attempts WHERE quiz_id=$1 AND status!='in_progress' AND live_rating>COALESCE($2,0)`,[req.params.quizId,rating.rows[0].rating]);res.json({success:true,rating:rating.rows[0].rating,rank:rank.rows[0].rank})}catch(e){next(e)}});
module.exports=router;