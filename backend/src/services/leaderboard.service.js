const { query } = require('../config/db');
const getLeaderboard = async ({ type='overall',categoryId,period='all',domainId }={})=>{const params=[];let idx=1;let dateFilter='';if(period==='weekly')dateFilter=`AND a.completed_at>=NOW()-INTERVAL '7 days'`;else if(period==='monthly')dateFilter=`AND a.completed_at>=NOW()-INTERVAL '30 days'`;let categoryJoin='',categoryWhere='';if(type==='category'&&categoryId){const parsed=parseInt(categoryId,10);if(!isNaN(parsed)){categoryJoin='JOIN quizzes qz ON qz.id=a.quiz_id';categoryWhere=`AND qz.category_id=$${idx}`;params.push(parsed);idx++}}let domainJoin='',domainWhere='';if(domainId){domainJoin='JOIN quizzes dq ON dq.id=a.quiz_id JOIN categories dc ON dc.id=dq.category_id';domainWhere=`AND dc.domain_id=$${idx}`;params.push(domainId);idx++}const sql=`SELECT u.id,u.name,u.avatar_url,u.bio,COUNT(a.id)::int quizzes_completed,COALESCE(AVG(a.percentage),0)::numeric(5,2) average_score,COALESCE(MAX(a.percentage),0)::numeric(5,2) highest_score FROM users u JOIN attempts a ON a.user_id=u.id AND a.status!='in_progress' ${dateFilter} JOIN quizzes aq ON aq.id=a.quiz_id AND COALESCE(aq.is_live_quiz,FALSE)=FALSE ${categoryJoin} ${domainJoin} WHERE u.role='STUDENT' AND u.status='active' ${categoryWhere} ${domainWhere} GROUP BY u.id,u.name,u.avatar_url,u.bio HAVING COUNT(a.id)>0 ORDER BY average_score DESC,quizzes_completed DESC LIMIT 50`;const result=await query(sql,params);return result.rows.map((row,index)=>({rank:index+1,...row}))};module.exports={getLeaderboard};